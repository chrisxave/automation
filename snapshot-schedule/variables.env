# Environment configuration for VM snapshot automation

# Name of the VirtualMachine to snapshot
VM_NAME=fedora01

# Namespace where the VM and CronJob will reside
NAMESPACE=vmexamples-chris

# Retention policy - how many snapshots to keep
RETENTION=5

# Schedule for the CronJob (in cron format)
SCHEDULE=*/15 * * * *
